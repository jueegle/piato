<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/question_radio_option"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="@dimen/margin_4dp"
    android:paddingBottom="@dimen/margin_4dp"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/radio_text_unselected"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/placeholder_short_text"
        android:textColor="@color/black"
        android:textSize="@dimen/text_size_20sp"
        android:gravity="center"
        android:padding="@dimen/margin_8dp"
        android:minLines="2"
        android:background="@drawable/shape_option_unselected"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <TextView
        android:id="@+id/radio_text_selected"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/placeholder_short_text"
        android:textColor="@color/white"
        android:textSize="@dimen/text_size_20sp"
        android:gravity="center"
        android:padding="@dimen/margin_8dp"
        android:minLines="2"
        android:background="@drawable/shape_option_selected"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:visibility="gone" />
</androidx.constraintlayout.widget.ConstraintLayout>